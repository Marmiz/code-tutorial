export const challengeTitle=`Create the elements for the Navbar`

export const challengeDescription= `As you read in the [flexbox article](https://css-tricks.com/snippets/css/a-guide-to-flexbox/) it's important to define clearly what is a parent container and what will be the children elements.

To do so we will use \`div\` to separate contents. Here's an example of 3 child elements separated from each other and their parent.

\`\`\`
<div>
  <div> 1 </div>
  <div> 2 </div>
  <div> 3 </div>
</div>
\`\`\`

Now go ahead and create a parent div with \`id="parent"\` and add three child elements in it.

`//closing

export const seedHtml = `<!--html-->`

export const seedCss = `/*css*/`
